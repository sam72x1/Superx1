# خطة أوبس‑7 — تنفيذ نتائج التدقيق العميق المُتحقَّق منها (المنفّذ: أوبس · المشرف: فيبل)

**الفرع:** `claude/bot-creation-xmjmr1` · **التاريخ:** 2026‑07‑12

> ⚠️ **اقرأ `CLAUDE.md` كاملًا + `IMPROVE_DEEP_AUDIT_REPORT.md` قبل أي سطر كود.**
> خطة أوبس‑6 (ت2) **نُفّذت ودُمجت** (PR #10 → `main` = `001a127`).
> المرجع التفصيلي لكل بند أدناه هو **تقرير التدقيق** (أرقام BUG-xx) — لا
> أكرّر أدلّته هنا؛ أضيف فقط حكم تحقّق المشرف وقيود التنفيذ والترتيب.

---

## 0. حكم المشرف: تحقّقتُ من **كل** النتائج الـ25 مقابل الكود — **25/25 مؤكّدة**

فحصتُ كل ادّعاء على المصدر الفعلي (مواضع الأسطر، وتاريخ git لـBUG-01
بإعادة تشغيل `git show dd7dd34`/`5a9ab43` بنفسي). لا نتيجة واحدة مرفوضة.

**فروق تأطير مهمّة اكتشفها التحقّق (تقيّد التنفيذ):**

1. **BUG-01 كامن لا نشط على قرصك الحي**: تتبّعك يعمل اليوم (13 تنبيهًا
   محسومًا) → قاعدة Render أُنشئت بعد `5a9ab43`. الإصلاح واجب (§7) لكنه
   وقائي — لا حريق.
2. **BUG-04 وBUG-10 ليسا «إصلاحًا صامتًا»**: تعريف الجاهزية (بشمعة اليوم
   الجزئية) وتعريف VWAP (يوم كامل) هما **نفس التعريفين اللذان تحقّقت بهما
   نتائج الستة أشهر (+2.91%)** — الباكتيست يطابق الحي فيهما (التعليق في
   `backtest.py:85-92` يوثّق المطابقة عمدًا). تغييرهما يغيّر الدرجات
   والبوّابات ويُبطل خط الأساس → **قرار مستخدم + إعادة باكتيست، ليسا في
   نطاق أوبس الآن** (انظر «قرارات المستخدم» آخر الملف).
3. **BUG-11 وBUG-07 (الشقّ السلوكي)**: جعل RVol المجهول يمرّ بخصم بدل
   الرفض يغيّر مَن يُنبَّه → الشقّ السلوكي **قرار مستخدم** أيضًا. الشقّ
   الآمن الوحيد (إزالة حقن artifact في الحساب) داخل ح2 أدناه.
4. **BUG-14 أثره الفعلي منخفض** (تنبيهات فقط، والبوّابات تمنع القيم الدنيا
   عادة) لكنه صحيح ورخيص — يُصلح.

---

## قواعد تنفيذ ملزمة لكل الحزم

- **commit منفصل لكل بند** برسالة عربية (ماذا+لماذا+رقم BUG).
- كل بند: اختبار يعيد الإنتاج أولًا حيث أمكن (§9)، ثم الإصلاح، ثم
  `python3 -m pytest runner_scanner/tests/ -q` أخضر كاملًا.
- **صفر تغيير في أي عتبة فرز أو سلوك انتقاء** — أي بند يلمس ذلك مؤجَّل
  لقرار المستخدم.
- طابِق أسلوب الملفات (تعليقات عربية · فواصل `─`).

---

## ح1 — أساس التحقّق (نفّذها أولًا — كل ما بعدها يعتمد عليها)

**[DX-01]** `requirements.txt`: أنشئ `requirements-dev.txt` فيه
`pytest==9.1.1` (لا تثبّته في بناء Render — الإنتاج لا يحتاجه).
أنشئ `.github/workflows/test.yml`: على push+PR → setup-python 3.11 +
`pip install -r requirements.txt -r requirements-dev.txt` +
`python3 -m pytest runner_scanner/tests/ -q`.
صحّح أمر §9 في `CLAUDE.md` و`README.md` إلى `python3 -m pytest`.
غيّر `buildCommand` في render.yaml إلى
`pip install -r requirements.txt && python -c "import runner_scanner.main"`
(فحص استيراد يحوّل انهيار التشغيل إلى فشل بناء).
وثبّت `PYTHON_VERSION` في render.yaml (§6 من قسم التبعيات — أرخص تأمين).
**أول أمر تنفّذه بعد بناء الـCI محليًا: شغّل المجموعة كاملة وأبلغ النتيجة
— التقرير لم يستطع تشغيلها.**

## ح2 — الانهيار الصامت والسلامة (S، خطر منخفض)

- **[BUG-01]** أضف الأعمدة الستة لحلقة الترحيل (`state.py:144`):
  `target2 REAL`, `target3 REAL`, `notified_targets INTEGER DEFAULT 0`,
  `notified_stop INTEGER DEFAULT 0`, `notified_high REAL`,
  `result TEXT DEFAULT ''`. استخرج tuple الترحيل لثابت وحدة
  `_MIGRATIONS`، واكتب اختبارَي التقرير: (قاعدة DDL قديم → فتح Store →
  كل أعمدة `_SCHEMA` موجودة) + (اتّساق ذاتي `_SCHEMA` مقابل
  «الأعمدة الأصلية المجمّدة عند dd7dd34» ∪ `_MIGRATIONS` — يمسك العمود
  المنسي القادم للأبد).
- **[BUG-05]** `esc(message)` في `monitor.py:40` + `esc(exc)` في رسالة
  «تعذّر الباكتيست التلقائي» (`main.py`). اختبار في `test_html_escape.py`.
- **[BUG-23]** سقف على نوم 429: `time.sleep(min(ra or backoff, 30.0))`.
- **[BUG-25]** `_handle_signal` يكتفي بـ`self._stop.set()`؛ `shutdown()`
  يُحرس بعلم idempotency (تنفيذ واحد).
- **[BUG-06]** انقل كتلة `save_champions` قبل رجوع البريماركت في
  `run_cycle` (الأبطال رصدٌ لا تنبيه — لا يخالف تعطيل تنبيهات البريماركت)،
  وشدّد `get_session_champions` في حالة الإزاحة 0 ليطلب اليوم بالضبط
  (الغياب → فارغ، لا بعث يوم قديم). اختبارا التقرير.
- **[BUG-07 — الشقّ الآمن فقط]** احذف `or snap.day_volume` في
  `intraday_ta.py:74` (يبقى الحساب من شموع الجلسة وحدها؛ **بلا** تغيير
  معالجة الصفر في البوّابة — ذلك قرار مستخدم). ملاحظة: في الأفترهاوس هذا
  يصحّح انتفاخ RVol المزيّف (حجم يوم كامل ÷ أساس أفترهاوس).

## ح3 — صدق القياس (الأرقام التي يُعاير عليها المستخدم)

- **[BUG-08]** في `update_outcomes`: افحص الوقف **قبل** حلقة الأهداف؛
  لو انطلق الاثنان في نفس النبضة → `result="loss"` (طابِق §8 والباكتيست).
  أبقِ إصدار حدثَي الرسائل كليهما. **أضف لرسالة الـcommit:** «ينقّص نسبة
  النجاح المسجّلة — انقطاع مقصود في السلسلة».
- **[BUG-16]** جمّد `high` بعد **أي** خروج محسوم في مسار
  `simulate_outcome` (الخسارة أيضًا، لا الفوز فقط). أبقِ `max_draw` كما
  هو (excursion مقصود) مع تعليق يوضّح.
- **[BUG-09]** اقتراحا الرفع يصفّيان الدليل بما **سيرفضه** المقترَح:
  `RVOL_MIN`: الشريحة `rvol < proposed` (لا `rvol_min+3`)،
  `ALERT_SCORE_MIN`: الشريحة `[lo, proposed)`. نمط
  `TECH_READINESS_MIN` القائم هو القدوة. حدّث نصَّي السبب.
- **[BUG-12]** وسّع `_run_config` بالحقول المؤثّرة (عدّدها صراحةً — لا
  `asdict`): `rvol_min`, `float_max`, `parabolic_day_change_pct`,
  `alert_score_min`, `trigger_change_pct`, `max_change_pct`,
  `min_target_profit_pct`, `catalyst_score_bonus`, `outcome_window_min`,
  `backtest_top_n`, `backtest_scan_step_bars`, `premarket_alerts_enabled`.
  (سطر التقرير `_fingerprint_line` يبقى مختصرًا كما هو — البصمة الكاملة
  في JSON للدمج.)
- **[BUG-14]** صحّح حدود شرائح `dev_assistant` لتطابق تسمياتها
  (`(5,8,"5-8x")`، وحدود الدرجة `(60,70,...)`).
- **[BUG-15]** صفوف `rejected and not is_alert` لا تُغلق على الوقف
  (استمرّ بتسجيل `hit_stop`/`max_draw` للمعايرة، والنافذة تغلقها) —
  كي لا يموت تنبيه 👻 الفرصة الفائتة قبل انطلاقه.

## ح4 — الهدر والاختبارات

- **[PERF-19]** كاش TTL للخبر: وسّع `DailyCache` بـ`get_ttl` ولُفّ
  `latest_news` في `pipeline.py`. عتبة جديدة `news_cache_ttl_sec=300.0`
  → **الأماكن الثلاثة** (§6).
- **[PERF-20]** مرّر `latest_news` في `AsOfClient` عبر `_cached` بمفتاح
  (سهم/يوم) مع تصفية محلية `published_utc <= asof` — مكافئ تمامًا لمرشّح
  الخادم، لا-تسرّب محفوظ.
- **[TEST-17]** `conftest.py` بـfixture تلقائية تمسح متغيّرات بيئة
  الماسح + اختبار جدولي `fields(Config)` مقابل `from_env()` بالبيئة
  الممسوحة + اختبار تطابق مفاتيح `.env.example` مع `from_env`. أضف
  المفاتيح التسعة الناقصة لـ`.env.example`. **توقّع أحمر أول تشغيل — 
  أصلح ما يكشفه (هذا هو الغرض) وأبلغ المشرف بما وجدت.**
- **[DOC-21]** `README.md:42` → 65/100. `VERIFY_NEXT_WEEK.md` §1 → صياغة
  «محسومة: رُفعت إلى 65؛ المفتوح الآن هل 65→70». أضف لـ`CLAUDE.md` سطر
  «العتبات تعيش في config.py وأي وثيقة تقتبس رقمًا تستشهد به».

## ح5 — بعد ح1–ح4 (أكبر حجمًا — كومِتات مستقلة)

- **[BUG-02]** مجمّع الباكتيست as-of: أبقِ أرضية قمة-اليوم كمرشّح أولي،
  وانقل الترتيب/القطع (top_n_runners) إلى داخل حلقة الشموع على التغيّر
  اللحظي `(bar.c - pc)/pc`. اختبار لا-تسرّب: سهم A أعلى عند T انتهى
  صغيرًا، وB انفجر لاحقًا → مجمّع T يحوي A لا B.
  **حذّر في رسالة الـcommit: كل الأرقام التاريخية ستتحرّك — خط أساس جديد.**
- **[DEBT-13]** `reason_code` في `GateResult`/`ScoreResult` + عمود جديد
  عبر `_MIGRATIONS` (بعد BUG-01) + تحويل المستهلكين الأربعة، والنص
  العربي للعرض فقط. اختبار «كل مسار رفض ينتج كودًا لا يقع في (أخرى)».
- **[SEC-22 — شقّ التحصين فقط]** محدِّدات `<news>` + تعليمة «بيانات لا
  تعليمات» في مطالبة المحلّل + تحقّق `direction` مقابل enum (قيمة غريبة
  → إسقاط النتيجة، best-effort §3). *(الحارس القاعدي الحتمي = قرار
  مستخدم — مؤجَّل.)*
- **صغائر مؤكّدة من §3 بالتقرير** (كومِت واحد «نظافة»):
  `esc()` لحقلي التخفيف في `alerts.py:120-122` ·
  `shutil.rmtree` لمجلّد CSV المؤقت في `dev_assistant` ·
  `_finra_cache` يحتفظ بأحدث جدول فقط ·
  إغلاق ws القديم قبل إعادة الاتصال في `halts.py` ·
  `f"{v:g}"` بدل `str(int(v))` في `backtest_grid`/`backtest_notes`.

---

## قرارات المستخدم — **لا تنفّذها؛ اعرضها** (تغيّر سلوك فرز/تنبيه)

1. **BUG-04**: حساب الجاهزية على المغلقة فقط (يتحرّك كل خط الأساس؛
   يتطلّب تعديل الحي+الباكتيست معًا وإعادة باكتيست 6 أشهر).
2. **BUG-10**: VWAP بنافذة الجلسة (نفس الشرط — بوّابة VWAP اعتُمدت
   بالتعريف الحالي).
3. **BUG-11 + شقّ BUG-07 السلوكي**: `rvol_reliable` والمرور بخصم بدل
   الرفض عند غياب الأساس.
4. **SEC-24**: `TELEGRAM_OWNER_ID` (يتطلّب خطوة نشر من المستخدم).
5. **PERF-18**: تضييق اشتراك `T.*` — **قِس أولًا** على Render.
6. **معرّفا نماذج Anthropic** (قسم التبعيات): تحديث `analyst_model`
   للاسم المستقر + فحص Anthropic في preflight + ترقية العطل عند 404/401
   — الشقّ الأول قرار كلفة للمستخدم، والشقّان الآخران آمنان ويمكن ضمّهما
   لح5 لو وافق المستخدم.

## خارج النطاق (درسها التقرير والمشرف — لا تلمسها)

- أي عتبة فرز (RVOL_MIN، ≥40%، …) — مدحوضة/تنتظر بيانات حية.
- linters/formatters/mypy/facades — التقرير نفسه يرفضها محقًّا.
- تقليم اختبارات السلاسل الكاشفة — «انتهازيًا عند أول كسر، لا كمشروع».
